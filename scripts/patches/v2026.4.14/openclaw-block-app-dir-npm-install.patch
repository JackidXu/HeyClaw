diff --git a/src/agents/apply-patch.ts b/src/agents/apply-patch.ts
index d6119cc4e1..7e4c067bd3 100644
--- a/src/agents/apply-patch.ts
+++ b/src/agents/apply-patch.ts
@@ -262,7 +262,28 @@ function resolvePatchFileOps(options: ApplyPatchOptions): PatchFileOps {
       }
     },
     writeFile: async (filePath, content) => {
+      const isAppOrGatewayPath = (p: string): boolean => {
+        if (!p) return false;
+        const absPath = path.resolve(p).replace(/\\/g, "/").toLowerCase();
+        if (absPath.includes("/applications/heyclaw.app")) return true;
+        if (
+          absPath.includes("/program files/heyclaw") || 
+          absPath.includes("/programs/heyclaw") || 
+          absPath.includes("/appdata/local/heyclaw")
+        ) {
+          return true;
+        }
+        if (absPath.includes("/cfmind")) return true;
+        if (absPath.includes("/vendor/openclaw-runtime")) return true;
+        return false;
+      };
+
       if (!workspaceOnly) {
+        if (isAppOrGatewayPath(filePath)) {
+          throw new Error(
+            `Security Exception: Modifying, writing, or deleting files inside the application package or gateway resource directory is strictly forbidden.`
+          );
+        }
         await fs.writeFile(filePath, content, "utf8");
         return;
       }
@@ -275,7 +296,28 @@ function resolvePatchFileOps(options: ApplyPatchOptions): PatchFileOps {
       });
     },
     remove: async (filePath) => {
+      const isAppOrGatewayPath = (p: string): boolean => {
+        if (!p) return false;
+        const absPath = path.resolve(p).replace(/\\/g, "/").toLowerCase();
+        if (absPath.includes("/applications/heyclaw.app")) return true;
+        if (
+          absPath.includes("/program files/heyclaw") || 
+          absPath.includes("/programs/heyclaw") || 
+          absPath.includes("/appdata/local/heyclaw")
+        ) {
+          return true;
+        }
+        if (absPath.includes("/cfmind")) return true;
+        if (absPath.includes("/vendor/openclaw-runtime")) return true;
+        return false;
+      };
+
       if (!workspaceOnly) {
+        if (isAppOrGatewayPath(filePath)) {
+          throw new Error(
+            `Security Exception: Modifying, writing, or deleting files inside the application package or gateway resource directory is strictly forbidden.`
+          );
+        }
         await fs.rm(filePath);
         return;
       }
diff --git a/src/agents/bash-tools.exec-runtime.ts b/src/agents/bash-tools.exec-runtime.ts
index 6e5694a5b8..d5a24f582e 100644
--- a/src/agents/bash-tools.exec-runtime.ts
+++ b/src/agents/bash-tools.exec-runtime.ts
@@ -516,6 +516,72 @@ export async function runExecProcess(opts: {
   const startedAt = Date.now();
   const sessionId = createSessionSlug();
   const execCommand = opts.execCommand ?? opts.command;
+
+  // Block any dependency or filesystem write/delete commands targeted at Application package or gateway resource files
+  const isAppOrGatewayPath = (p: string): boolean => {
+    if (!p) return false;
+    const absPath = path.resolve(p).replace(/\\/g, "/").toLowerCase();
+    if (absPath.includes("/applications/heyclaw.app")) return true;
+    if (
+      absPath.includes("/program files/heyclaw") || 
+      absPath.includes("/programs/heyclaw") || 
+      absPath.includes("/appdata/local/heyclaw")
+    ) {
+      return true;
+    }
+    if (absPath.includes("/cfmind")) return true;
+    if (absPath.includes("/vendor/openclaw-runtime")) return true;
+    return false;
+  };
+
+  const hasGatewayOrAppPathInCommand = (cmd: string): boolean => {
+    if (!cmd) return false;
+    const cleanCmd = cmd.toLowerCase().replace(/\\/g, "/");
+    if (
+      cleanCmd.includes("/cfmind") || 
+      cleanCmd.includes("vendor/openclaw-runtime") ||
+      cleanCmd.includes("/applications/heyclaw.app") ||
+      cleanCmd.includes("/program files/heyclaw") ||
+      cleanCmd.includes("/programs/heyclaw") ||
+      cleanCmd.includes("/appdata/local/heyclaw")
+    ) {
+      return true;
+    }
+    return false;
+  };
+
+  const hasSideEffectCommand = (cmd: string): boolean => {
+    if (!cmd) return false;
+    const cleanCmd = cmd.toLowerCase();
+    const writePatterns = [
+      /\bnpm\s+(install|i|add|update|uninstall|link|publish)\b/,
+      /\bpnpm\s+(install|i|add|update|uninstall|link|publish)\b/,
+      /\byarn\s+(install|add|update|uninstall|link|publish)\b/,
+      /\b(rm|mkdir|touch|cp|mv|chmod|chown|ln|sed|tee)\b/,
+    ];
+    if (writePatterns.some(pattern => pattern.test(cleanCmd))) {
+      return true;
+    }
+    if (cleanCmd.includes(">")) {
+      return true;
+    }
+    return false;
+  };
+
+  if (
+    (isAppOrGatewayPath(opts.workdir) || 
+     isAppOrGatewayPath(opts.command) || 
+     isAppOrGatewayPath(execCommand) ||
+     hasGatewayOrAppPathInCommand(opts.command) ||
+     hasGatewayOrAppPathInCommand(execCommand)
+    ) &&
+    (hasSideEffectCommand(opts.command) || hasSideEffectCommand(execCommand))
+  ) {
+    throw new Error(
+      `Security Exception: Modifying, writing, or deleting files inside the application package or gateway resource directory is strictly forbidden.`
+    );
+  }
+
   const supervisor = getProcessSupervisor();
   const shellRuntimeEnv: Record<string, string> = {
     ...opts.env,
diff --git a/src/agents/pi-tools.read.ts b/src/agents/pi-tools.read.ts
index fefe6bc28a..43c445973c 100644
--- a/src/agents/pi-tools.read.ts
+++ b/src/agents/pi-tools.read.ts
@@ -748,6 +748,28 @@ function createSandboxEditOperations(params: SandboxToolParams) {
 
 async function writeHostFile(absolutePath: string, content: string) {
   const resolved = path.resolve(absolutePath);
+  const isAppOrGatewayPath = (p: string): boolean => {
+    if (!p) return false;
+    const absPath = path.resolve(p).replace(/\\/g, "/").toLowerCase();
+    if (absPath.includes("/applications/heyclaw.app")) return true;
+    if (
+      absPath.includes("/program files/heyclaw") || 
+      absPath.includes("/programs/heyclaw") || 
+      absPath.includes("/appdata/local/heyclaw")
+    ) {
+      return true;
+    }
+    if (absPath.includes("/cfmind")) return true;
+    if (absPath.includes("/vendor/openclaw-runtime")) return true;
+    return false;
+  };
+
+  if (isAppOrGatewayPath(resolved)) {
+    throw new Error(
+      `Security Exception: Modifying, writing, or deleting files inside the application package or gateway resource directory is strictly forbidden.`
+    );
+  }
+
   await fs.mkdir(path.dirname(resolved), { recursive: true });
   await fs.writeFile(resolved, content, "utf-8");
 }
