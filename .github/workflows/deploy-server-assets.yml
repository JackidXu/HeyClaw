name: Deploy Server Assets to Aliyun OSS

on:
  push:
    branches:
      - main
    paths:
      - 'server-assets/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Upload Server Assets to Aliyun OSS
        uses: aliyun/oss-action@v1.0.2
        with:
          key-id: ${{ secrets.OSS_ACCESS_KEY_ID }}
          key-secret: ${{ secrets.OSS_ACCESS_KEY_SECRET }}
          bucket: ${{ secrets.OSS_BUCKET }}
          endpoint: ${{ secrets.OSS_ENDPOINT }}
          assets-dir: server-assets/
          target-dir: heyclaw/server-assets/

